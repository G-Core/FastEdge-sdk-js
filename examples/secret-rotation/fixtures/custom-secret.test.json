{
  "appType": "http-wasm",
  "description": "x-secret-name override — looks up SIGNING_KEY instead of TOKEN_SECRET",
  "request": {
    "method": "GET",
    "path": "/",
    "headers": {
      "x-secret-name": "SIGNING_KEY",
      "x-slot": "1741790697"
    }
  },
  "dotenv": {
    "enabled": true,
    "path": "."
  }
}
